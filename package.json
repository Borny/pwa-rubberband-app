{
  "name": "pwa-rubberband",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -r dist/ && parcel public/index.html public/pages/*.html",
    "build": "rm -r build/ && parcel build public/index.html -d build --public-url ./ --no-source-maps",
    "gh-pages": "git subtree push --prefix build origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "http-server": "^0.9.0",
    "sass": "^1.32.8"
  }
}
